{#<nav class="mb-5 navbar navbar-expand-lg">
    <div class="container">
        <div class="title pr-2 float-left">
            <a href="{{ path("default_index") }}" class="">Sortie.com</a>
        </div>
        <div class="float-right pt-2 pb-2">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="#" class="menu p-2">Villes</a>
                <a href="#" class="menu p-2">Sites</a>
                <a href="{{ path("default_index") }}" class="menu p-2">Accueil</a>
                <a href="{{ path("user_profil") }}" class="menu p-2">Mon profil</a>
                <a href="{{ path("security_logout") }}" class="menu p-2">Déconnexion</a>
            {% else %}
                <a href="{{ path("security_login") }}" class="menu p-2">Connexion</a>
            {% endif %}
        </div>
        <div class="clearfix"></div>
    </div>
</nav>#}
<nav class="navbar navbar-expand-lg">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon">X</span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand  mr-auto" href="{{ path("default_index") }}">Sortie.com</a>
            <ul class="navbar-nav  mt-2 mt-lg-0 text-light">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Villes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Sites</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>