{% extends 'base.html.twig' %}

{% block title %}Nouvelle sortie | {{ parent() }}{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Créer">
    <a href="{{ path("default_index") }}" class="btn btn-primary">Retour</a>
    {{ form_end(form) }}
{% endblock %}